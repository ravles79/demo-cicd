name: Run DecK
on: push
jobs:
  deck-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Setup deck
        uses: kong/setup-deck@v1
        with:
          deck-version: '1.16.0'

      - name: Clone repository
        uses: actions/checkout@v2

      - name: Run DecK sync
        run:
          deck gateway sync --kong-addr https://us.api.konghq.com --kong-token kpat_C1m8GIbvammDDQIc69bgEHgAksgnXDL4N7uQALbaP84Q3Wv9M
          echo "DecK sync completed successfully."
